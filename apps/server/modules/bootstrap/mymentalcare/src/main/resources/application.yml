server:
  port: 3001

spring:
  application:
    name: my-mental-care
  profiles:
    include:
      - datasource

management:
  endpoints:
    web:
      exposure:
        include: health,info

springdoc:
  swagger-ui:
    path: /swagger-ui/index.html
