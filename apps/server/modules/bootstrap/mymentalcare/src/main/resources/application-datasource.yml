spring:
  datasource:
    url: jdbc:mariadb://localhost:3310/mymentalcare
    username: mymentalcare
    password: mymentalcare-local
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
  data:
    redis:
      host: localhost
      port: 6380
